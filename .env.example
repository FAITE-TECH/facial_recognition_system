# Copy this to .env and fill in your own values
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=class_attendance

# Face config
# Default cosine similarity threshold (0.38 is a decent starting point; calibrate per environment)
FACE_THRESHOLD=0.38
SAVE_IMAGES=true
IMAGE_DIR=static/face_images
